package compiler;

/**
 * Runs the semantic analysis.
 */
public class Semantic {

    public void executeAction(int action, Token token)	throws CompilationError {
        throw new CompilationError("Not implemented yet.", -1);
    }
}